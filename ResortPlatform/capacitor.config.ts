import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.hospify',
  appName: 'Vestlia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
